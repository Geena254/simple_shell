#include "shell.h"

/**
 * sub - Function that subtracts one value from another
 * @a: the first value to use
 * @b: the second value to subtract from a
 * Return: the subtraction result
 */

int sub(int a, int b)
{
	return (a - b);
}
